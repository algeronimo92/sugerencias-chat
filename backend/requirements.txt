fastapi==0.115.0
uvicorn[standard]==0.30.6
httpx==0.27.2
asyncpg==0.29.0
SQLAlchemy[asyncio]==2.0.51
# Va en producción, no sólo en desarrollo: scripts/migrate.py se ejecuta desde
# la imagen del backend durante el despliegue, antes de arrancar la aplicación.
alembic==1.18.5
python-dotenv==1.0.1
pydantic-settings==2.5.2
bcrypt==4.2.1
PyJWT==2.9.0
cryptography==49.0.0
minio==7.2.20
# Dimensiones de imágenes recibidas: el frontend reserva el espacio exacto
# antes de que carguen, para que el chat no se mueva (placeholders).
Pillow==11.1.0
# La zona horaria del negocio (America/Lima) se resuelve con zoneinfo, que en
# Windows y en imágenes slim no trae la base de datos de zonas del sistema.
tzdata==2024.2
# Expone /metrics para Prometheus: histogramas por ruta/método listos
# (instrumentator) más los gauges propios de services/queue_metrics.py
# (prometheus_client). No reemplaza services/performance.py, que sigue
# emitiendo Server-Timing para devtools.
prometheus-client==0.21.1
prometheus-fastapi-instrumentator==7.0.0
